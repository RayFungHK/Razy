# Razy\SimpleSyntax

## Summary
- Utility for parsing delimited syntax with nested parentheses.

## Key methods
- `ParseSyntax($syntax, $delimiter, $negativeLookahead, $parser, $notCaptureDelimiter)`.
- `ParseParens($text)`.

## Usage notes
- Used by Where/TableJoin/update syntax parsing.
